import 'package:flutter/material.dart';

class ChatBoy extends StatelessWidget {
  const ChatBoy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 100,

    );
  }
}
